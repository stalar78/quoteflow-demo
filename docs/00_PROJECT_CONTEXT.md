# 00. Контекст проекта

QuoteFlow — самостоятельный проект в локальном каталоге `C:\Users\stala\OneDrive\Рабочий стол\Dev\QuoteFlow` и отдельном GitHub-репозитории `stalar78/quoteflow-demo`. Он не является частью `site-stalarvision` и не должен изменять файлы основного сайта.

## Назначение

QuoteFlow задуман как калькулятор сметы и генератор коммерческого предложения для малого бизнеса, экспертов и небольших команд. Продукт одновременно решает понятную прикладную задачу и демонстрирует компетенции Stalar Vision в бизнес-калькуляторах, web-интерфейсах, REST API, обработке структурированных данных, PDF generation и тестировании бизнес-логики.

## Владение и позиционирование

Владелец проекта:

- Ларин Станислав;
- Stalar Vision;
- независимый разработчик / ИП.

Публичная формулировка: «Собственный демонстрационный продукт Stalar Vision».

QuoteFlow нельзя выдавать за клиентский проект. Допустимо описывать его как собственный продуктовый пример, demo или case study без утверждений о внешнем заказчике.

## Запрещённые утверждения

Нельзя придумывать клиентов, отзывы, статистику, коммерческие результаты, экономию времени, количество документов, uptime, окупаемость, цены разработки, гарантии или другие неподтверждённые показатели.

## Принятые решения

- Рабочее название: `QuoteFlow`.
- Репозиторий: `stalar78/quoteflow-demo`.
- Репозиторий был публичным с момента создания.
- Лицензия проекта: MIT, Copyright (c) 2026 Stanislav Larin (Stalar Vision).
- Frontend: React, TypeScript, Vite, Tailwind CSS, Vitest, Testing Library.
- Backend: Python, FastAPI, Pydantic, pytest.
- Черновики хранятся только в браузере через `localStorage`.
- Backend остаётся stateless.
- Server database в MVP отсутствует.
- Платежи, CRM, реальные webhook/email/Telegram-интеграции, AI и пользовательские аккаунты не входят в MVP.
- Production target: существующий Beget VPS, Ubuntu, Docker и системный Nginx; фактический deployment требует отдельного resource/security review.

## Ограничение данных

Публичное demo не предназначено для реальных персональных, коммерческих или конфиденциальных данных. Для разработки, тестов, screenshots и демонстрации используются только synthetic/demo data.

## Текущий статус

Этапы 0–6 и Stage 7A завершены. Stage 7B находится на этапе подготовки deployment-конфигурации для `https://quoteflow.stalarvision.ru/` на существующем Beget VPS. Репозиторий публичный с момента создания; MIT License согласована и добавлена. Live deployment не выполнялся.
